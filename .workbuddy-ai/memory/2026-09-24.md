# 2026-09-24 · 工作日志

## 三轮递进式 Debug 审计（只诊断，未修任何 Bug）

按 `docs/audit-prompt.md` 对全仓做满三轮（模式库扫捕 → 六轴扩张 → 跨层与边界），产出
`docs/debug-audit-report-2026-09-24.md`（46KB，含文件:行 / 复现命令 / 根因链 / 排除证据 / 建议方向）。

**基线**：`c361d8a`，工作树只有 1 个 stat-dirty（`apps/api/src/backup.ts`，blob 哈希与 HEAD 相同）+ 3 个未跟踪计划文档。
Node 24.20.0 `npm run typecheck` PASS、`npm test` **98/98** PASS。全程未写 `data/`、未连 3011/5199。

**18 项发现：P0 0 / P1 3 / P2 5 / P3 10。** 三个 P1 全在「保护自己的那套东西」上：

- **A-01** 生产守卫拿 `/api/backup/status` 的 `localDirectory`（= `config.backupDirectory`，可被
  `LIFEOS_BACKUP_DIR` 独立指定）判断「是不是主人的库」⇒ 备份目录挪到 `data/` 之外就静默放行。
  `verify-production-guard.mjs:88` 的「放行」夹具正是这个形态，**所以回归测试永远抓不到**。
- **A-02/A-03/A-16** `audit-toolbox-targets.mjs` 的「UNGUARDED = 0」不是安全证明：它只认
  `3011`/`5199` 与 `method:"POST"` 两个字面量。`verify-privacy.mjs`（走 3012 + `LIFEOS_VERIFY_BASE`）
  与 `verify-composer-shots/pickers.mjs`（CDP 打字 + 点保存）都被归进「只读」桶，而它们真会写。
  生产库现存 5 条 `验收：照片和文字一起保存（可删）` 软删墓碑 = 实证。

**实测复现的**：A-04 雾带正 `animation-delay` 导致延迟期内 opacity 1 → 跳变（CDP 量到 1 → 0.503/0.391）；
A-10 Markdown 导出完全不含 `note`；A-09 `revision` 根本不在导出契约内。

**新踩的环境坑（重要）**：按 `AGENTS.md` 现有写法跑 `npm test` **会失败**
（`node: bad option: --test-isolation=none`，退出码 9）。光用 `C:\Program Files\nodejs\node.exe`
的绝对路径不够 —— `npm run test` 内层的 `node --test` 走 **PATH**，而本机 PATH 里 managed 的
22.22.2 排在 24.x 前面。**必须先 `export PATH="/c/Program Files/nodejs:$PATH"`。**

**新增隔离诊断产物**（都在 `.review/`，只读或自带实例）：`_audit-inspect.mjs`、
`_audit-prod-residue.mjs`（`readOnly:true` 开生产库，只输出计数/日期键）、`_audit-r2-writers.mjs`、
`_audit-r3-roundtrip.mjs`、`_audit-r3-fog.mjs`。本次产生的 profile 与临时数据目录已自删
（`.review/profiles/` 仍剩 9 个更早会话的残留，未动）。

交接记录已按 `AGENTS.md` 追加：`docs/todo.md` §24、`docs/changelog.md` 末尾一条（署名 WorkBuddy）。
**未 commit、未 push。**

---

## 接力收尾：账户模式欢迎流程 + 邀请码 + 天气跟随 + 移动端「更多」（在下 / WorkBuddy，2026-09-24 傍晚）

上一棒（Luna Max/GPT-6）把账户模式的数据层写完了（`identity-store.ts` 的邀请码、`WelcomeGate.tsx`、
`weather-follow.ts`、`welcome.css`）但**没接线**就额度耗尽。本轮把线全部接上并验收：

**接了什么**：`server.ts` 增邀请码 HTTP 入口（`invite/check`、`register` 事务内消费、owner 的
`admin/invites` + `revoke`）；`main.tsx` 渲染 `WelcomeGate` 并挂 `useWeatherAutoFollow`；
`settings-cards.tsx` 增 owner 邀请码管理卡与「跟随当前位置」开关；`tenant-config.ts` 的
`SharedIntegrations` 把 owner 的 AI/天气 Key 下发给成员（**位置不继承**）；`app-meta.ts` 的 AI 助手默认关。

**管理员判定（主人第一个问题）**：owner = 服务端身份库的角色，**不看用户名**；兑换邀请码一律 `member`，
**第一个兑换的人也不会变成 owner**。

**两个真正花时间的坑**：
1. **`.mobile-menu-dialog` 与 `.modal-dialog` 共用一条规则**（上一棒的写法）⇒ 把 `inset: auto` /
   `margin: 0` / `width: 264px` 套到了**所有模态框**上（它们靠 UA 的 `inset:0 + margin:auto` 居中）。
   拆分后才既让「更多」成为贴标签的 popover、又不把搜索/导入/确认框拽到左上角。
2. **`.review/verify-mobile-layout.mjs` 早就烂了，不是本轮改坏的**：它用 `*api/weather*` 把所有天气
   请求换成硬编码 `2026-09-20` 的快照夹具，把 `/api/weather/profiles`（早就在 `routes-ai-movie.ts:208`）
   也换成快照 → 页面显示「天气未配置」，**第一项就超时**。顺手修了四处陈旧判据：只替换 `/api/weather`、
   夹具日期动态化、`.calendar-mode-button` → `.mode-option`（前者**从未存在过**）、周模式 14 张卡要写成
   `.week-grid:not(.week-grid-prev) .week-card:not(.is-prev)`、日期选择器只在非日历视图渲染所以要先把
   「回今天」提到写值之前。改完 43/43 PASS（它此前从没跑完过）。

**验收数字**：core 29 / api 64 / web 11 / instance-backup 4 = **108**；`verify-account-welcome-ui.mjs`
**39/39 PASS**（自带隔离实例，不碰 `data/`）；`verify-mobile-layout.mjs` 只读对 3011/5199 **43/43 PASS**。
**未 push**（按规则由主人决定）；`data/` 一个字节没动。

**留着的东西**：3013 的账户模式预览（`.review/account-preview-serve.mjs`，owner/lifeos-preview-owner）
方便走一遍欢迎流程；3011/5199 照常运行。已知仍红的 6 条（asset-trash / composer-shots / hash-dedup /
search-ui / thumbnails / probe-search-click）是上一轮就红的，没动。

### 收尾状态：**两个提交都还没 push**（20:3x，在下）

- `main` 比 `origin/main` **ahead 2**：`6c787f3`（MiSans 字体收敛，Codex 提交）+ `c09a36a`（本轮账户模式收尾）。
  远端 `https://github.com/Tsang12140/lifeos.git`；`credential.helper=store`（凭据在 `~/.git-credentials` 明文，
  所以 `git push` 不弹窗；报 403 就是里面的 token 过期了）。
- **服务器更新口径**（`README.md:145-175`）：宝塔反代上游 `http://127.0.0.1:3001`；服务器上
  `git pull` 之后必须 `docker compose up -d --build` 重建镜像。**LifeOS 目前还没在服务器上真正部署过**
  （todo §23 第 5 条「未执行」），所以「宝塔拉取」这条路在 LifeOS 上是一次都没跑过的。
- **交接链永远不上服务器**：`.gitignore` 排除了 `AGENTS.md`、`docs/changelog.md`、`docs/todo.md`、
  `.review/`、`data/`、`pic-test/`、`.env`、`*.sqlite`。→ 服务器 `git pull` 之后**看不到**规则/日志/待办/验收脚本，
  也**不会**带上主人的数据。注意 `.workbuddy/` 被忽略，但 **`.workbuddy-ai/` 是被跟踪的**（所以 MEMORY.md 进库）。
- 未跟踪（也不会随 push 上去）的 4 份文档：`docs/date-switch-execution-plan.md`、
  `docs/debug-audit-report-2026-09-24.md`、`docs/mobile-layout-execution-plan.md`、`docs/notes-library-execution-plan.md`。

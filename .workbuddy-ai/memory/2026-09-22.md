# 2026-09-22（承接 09-21 深夜那条线）

## 观影模块「让剩余流程跑通」收尾

- **做完了什么**：给观影模块补上**浏览器端到端验收**（09-21 那条只验了命令行）。
  新脚本 `.review/verify-movie-e2e.mjs`（**只读**），34 条断言：
  1. 设置页 `#settings/integrations/movie` 观影卡片：状态标签 **已连接** + `is-ready`、Key 框 placeholder **「已保存，留空不变」**、开关已勾。
  2. **在界面里点「测试连接」** → 「TMDb API 连接成功」（走 Vite `/api` 代理 ⇒ 浏览器这条通路也通）。
  3. **真识别**：首页 composer 打 `/` → 「电影」→ 面板 → 「盗梦空间」→ 识别 → 3 个候选、首位《盗梦空间》、海报 `image.tmdb.org`、首张滚动后 `naturalWidth=500`。
- **不落盘怎么保证**：只调 `GET /api/movie/status`、`POST /api/movie/config/test`、`POST /api/movie/resolve`（都不写）；**从不点「添加到记录」**（那才是 import，会建实体）。结尾用 `GET /api/records` / `GET /api/entities` 前后对比自证：149→149 / 52→52 / 电影 2→2。
- **结果**：`RESULT: PASS (34 checks)`，连跑 4 次（含冷启动）全绿。`.review/verify-movie-e2e.txt`。

## 本轮新踩到的坑（都已进 changelog / MEMORY）

1. **异步取回的状态会让「刚挂载的卡片」骗过断言**：`main.tsx:3710` 里 `movieStatus` 初值是 `{enabled:false, connected:false, …}`，真值异步到。第一版没等就断言 → 4 条假 FAIL（而同一份卡片文字 dump 里明明写着「已连接」）。**先 `waitFor` 状态落位再断言。**
2. **CDP 的 `click(expr)` 要收「求值为元素的表达式」，不是 CSS 选择器**。裸选择器塞进 `const el = ${expr}` = 页面抛语法错 → `Runtime.evaluate` 回 undefined → 报 `MISS`，而 `querySelector` 明明查得到。现在用 `$("…")` / `buttonByText("容器","文字")`。
3. **只差 hash 的 `Page.navigate` 是同文档导航** → 不能指望视图自己切回来（与 09-21 那条同源）。
4. **`reapChromeSync` 返回 true ≠ 目录真没了**：本轮早期 4 个 profile（p2rlUF / kvIuq2 / QUhzft / J9khqD）在报「已删除」的情况下留在 `.review/profiles/`。**根因没复现**（清空后连跑 3 次 + 冷启动，每次 `existsSync=false`、剩 0 个）。已手工删掉；脚本改成**自己报「运行期间新出现的 profile」+ 目录总数**，以后漏了藏不住。
5. **`node -e` 写含 `$(…)` 的文本会被 bash 当命令替换静默抹掉** —— 我在 changelog 里写 `$("选择器")` 就被吃成 `` `` ``（文件本身没坏，但少了一段）。**又违反了「先 Write 成文件再读」这条自己的规矩。** MEMORY.md 里那条已补上 `$(…)`。

## 收尾动作

- 清掉 Temp 里 10 个上一轮的一次性脚本（`lifeos-*.mjs`）与诊断探针 `.review/probe-reap-timing.mjs`。
- `.review/sweep-profiles.mjs` 双位置 **0 个 / 0.0 MB**；`audit-toolbox-targets.mjs` **0 个未守卫写入脚本**。
- `docs/changelog.md` 追加「## 2026-09-22 00:47」那条；产品代码本轮**零改动**。

---

# 2026-09-22（02:3x）主人问「9 月 17 号看《霸王别姬》没有海报」→ 查出是验收脚本污染生产库

## 结论：那条记录不是主人写的，是 `.review/verify-movie-module.mjs` 的夹具残留

三条证据：正文是脚本写死的（`今晚重看《霸王别姬》`）、对象 id `movie_verify_<时间戳>`、**海报是脚本在页内 mock 掉 TMDB 后自造的灰色 SVG 占位图**（画着「MOVIE」四个字）→ 所以「没有海报」是必然。短评写着 `验收之前的短评`。
老版本脚本**硬编码 5199/3011、通篇零个 DELETE** → 跑一次就永久往生产库写 3 条记录 + 1 个实体；2026-09-17 01:55 / 02:04 跑过两次。它躲过了 09-18 那轮守卫普查。

## 做完了什么

1. **清理**：新工具 `.review/cleanup-movie-verify-residue.mjs`（签名校验 + dry-run 默认 + 认不出一行就中止整轮）。`--apply` 后记录 2 条 **204**、实体 2 个 **204**；生产库 **149/52/2 → 147/50/0**。
2. **`purge-trash-junk.mjs` 补签名** `/^今晚重看《霸王别姬》$/`（该脚本的签名表一直漏了第三条正文，那两条墓碑永远清不掉）。dry-run `matched 7 / UNMATCHED 0`；**`--apply` 故意没跑** —— 主人选的是「可恢复」方案。
3. **`verify-movie-module.mjs` 重写成隔离 + 自清**：API 3071 / Vite 5392 / CDP 9262（可用 `LIFEOS_MOVIE_*_PORT` 覆盖），数据在 `.review/movie-module-run/`；起进程前探端口；收尾删掉自己建的记录与实体并回读复核。结果 **`RESULT: 1 FAIL (21 checks)`**，`MISS 0` / `TIMEOUT 0`，自清全 204，隔离库收尾 0 条。
4. 顺手修掉它三个潜在缺陷：① 设置页导航压根没发生（默认落 `account/session`，而 `[data-movie-settings]` 只在 `integrations/movie`）→ 显式点 `#settings-page-integrations-movie`；② `<input type="number">` 上调 `setSelectionRange` **抛异常** → 后面的 `dispatchEvent("input")` 不执行 → React onChange 永不触发 → 现在 `try/catch` + **回读校验**；③ 带判定语义的等待拆成 `waitUntil` / `waitFor`，不再一条失败计两次。崩溃处理器改到 `profile`/`chromeProc` 存在之后再注册（否则端口探测抛错会撞 TDZ）。

## 唯一那条 FAIL 已定位到行（产品缺陷，未修 → `docs/todo.md` §11）

- `main.tsx:2513` 把 `<MoviePrompt>` 塞进 `<p className="timeline-text">` **里面**；`movie.tsx:307` 的 `MoviePrompt` → `movie-prompt-popover` → `MovieAddPanel`，而后者的根是 **`<div className="movie-add-panel">`**（`movie.tsx:262`），里面有 **`<form>`**（:265）与 **`<p>`**（:266/:268）→ `<div>`/`<form>`/`<p>` 全成了 `<p>` 的后代。
- **为什么恰好 6 条**：React 按 `!!parentInfo + "|" + childTag + "|" + ancestorTag` **去重**（`react-dom-client.development.js:2759-2761`）→ 3 组合 × 2 条 = 6。
- 另一挂载点 `main.tsx:1049`（composer 的 `/电影`）外面是 `<div>` 链 → 不触发；5199 上的只读探针专走那条路，**4 个阶段 0 条 error**。
- 触发条件 `main.tsx:2507`：模块开着 + 正文含「电影」的日记 + 还没挂电影 → **主人的真实路径会踩**。

## 本轮新踩的坑

1. **React DOM 嵌套警告的祖先栈里，不带 props 的裸标签是「兄弟节点」，不是祖先**（`describeNode` → `describeSiblingFiber`，`react-dom-client.development.js:2486-2497`）。我第一眼把 `main.main-column > header` 当成链上节点，得出「警告在 topbar」的错误结论；实际 `header` 只是 `div.content-grid` 的**前一个兄弟**，链走的是时间线列。**读这类栈先分清「带 props 的」和「裸标签的」。**
2. **我自己的捕获代码把证据切了**：`.slice(0, 200)`，而这条消息到 `<header>` **正好 200 字符** —— 截断点太整齐，反而让「栈就到这里」看起来像事实。**诊断用捕获别设这种量级的上限。**
3. **`.review/audit-toolbox-targets.mjs` 靠 `/method:\s*["'](POST|PATCH|PUT|DELETE)["']/` 认「会写的脚本」** → 写 `send(path, "DELETE", body)` 会被归成「只读」。必须写成 `{ method: "DELETE", body }` 的字面量形状。

## 收尾

- `docs/changelog.md` 追加「## 2026-09-22 02:35」；`docs/todo.md` 新增 §11 并更新「最后更新」行。**产品代码本轮零改动**（只动 `.review/` 工具与 docs）。
- 删掉一次性探针 `.review/scan-movie-residue.mjs`。
- `audit-toolbox-targets.mjs`：`isolated 40 / will-write 19（guarded 15 + prod-tool 4 + UNGUARDED 0）`，退出码 0。

---

# 2026-09-22（02:52）主人选 D 路线 → 那 6 条非法嵌套修掉了（一行）

## 改了什么

**`apps/web/src/main.tsx:2513`**：`<p className="timeline-text">` → **`<div className="timeline-text">`**（闭合标签一起改）。**产品代码只动这一处。**

为什么一行就够（逐项核对过，不是推断）：样式与 `.review/` 里 12 个脚本**全按类选择器**挂钩（`styles.css:182/1960/6930`、`verify-selectability.mjs:44`）；唯二受标签影响的 `p { margin-top: 0 }`（`styles.css:1357`）与 UA 默认 margin **都被 `.timeline-text` 自己的 `margin: 9px 0 0` 覆盖**；`p` 的后代选择器都不在 `.timeline-text` 子树内；弹层锚在 `.movie-prompt-wrap`（自己 `position:relative`），不受影响。

## 验证数字

- `npm run typecheck` → core / api / web **三段全绿**。
- `node .review/verify-movie-module.mjs` → **`RESULT: PASS (21 checks)`**，`PASS zero console errors (0)`（原 `FAIL … (6)`），连跑 4 轮都 21/21。
- `node .review/verify-selectability.mjs` → **`RESULT: PASS (59 checks)`**，`.timeline-text` 四条断言全 ok（`user-select: text` + 拖蓝选出「看复联5」）。

## 顺带修掉的两处脚本收尾

1. **`child.kill("SIGKILL")` 在 Windows 上不保证进程已死** → `rmSync(RUN)` 一直失败（API 开着 sqlite，API/Vite 还继承了 `api.log`/`web.log` 句柄）。改成对子进程也走**同步 `taskkill /F /T /PID`**（新助手 `killTreeSync`）→ `movie-module-run` 再没留下过。
2. **profile 泄漏**：`reapChromeSync` 报成功、目录却会回来，**每轮漏约 50MB**（清掉 6 个共 **250.6MB**）。缓解：`sweepProfiles` 窗口 6h → **15min**；收尾「盯 3 秒、回来就再杀再删」；**自己列一遍 profile 目录再报数**。**根因未查清** → `docs/todo.md` §12。

## 这条 profile 之谜的实测结论（都写进 §12 了）

- 删除**确实发生**（连续 2.75s `exists=false`）；几秒后目录**又完整长回来**（115 目录 / 278 文件）；事后 `chrome.exe` = 0、`chrome_crashpad_handler.exe` = 0；同一目录稍后普通 `rmSync` 一次就删掉；3 秒的「盯住再删」抓不到（`reappeared=0`，但最终列表里仍是它）。
- **已否掉的假设**：「`spawn` 拿到的是启动器 pid、`taskkill` 打空」。探针实测 `spawn` 返回的 pid 就是主浏览器进程（10 个 chrome 进程都指向它）。探针文件跑完已删。
- **还没测到的**：目录重新出现的那一刻到底有没有 chrome 进程 —— 脚本内 `execFileSync("tasklist", …)` 抛错（`chrome=-1`），**抛的什么错没查**（外面手工调 tasklist 是好的）。这是下一步最该补的数。

## 收尾

- `docs/changelog.md` 追加「## 2026-09-22 02:52」；`docs/todo.md` §11 标 **已修**（保留原始记录）+ 新增 §12 + 更新「最后更新」行。
- `.review/profiles/` 清空（250.6MB）；一次性探针 `.review/_probe-chrome-pid.mjs` 已删。
- `MEMORY.md` 去重压缩：7892 → **6495 字符**（bytes 13783 → 11377），24 项关键规则抽查全部在位。

## 再压一轮：`MEMORY.md` 会**整份被截断**，尾部三节等于没有（03:0x）

- **触发**：本轮进场时系统提示 `MEMORY.md` 超限、注入时被截断 —— 截断点正好落在 `## UI 硬规则` 的「卡片宽度」那条，**后面三节（UI 硬规则 / 日历）根本没进上下文**，而且**连个指针都没有**。上一轮压到 6495 字符仍然超。
- **处置（不是删规则，是搬家）**：把 `## 时光机` / `## UI 硬规则` / `## 日历` 三节**逐字**移进 `MEMORY-detail.md` 新增的 `## 细则全文（2026-09-22 从 MEMORY.md 移入…）`；`MEMORY.md` 尾部换成一块**指针 + 每节头条规则**（含命令与阈值），并改文件头明说「本文件必须能整份注入」。
- **数字**：`MEMORY.md` 6749 → **5107 字符**（bytes 11834 → **8532**，−28%）；detail 36720 → 39496 字符。
- **校验**：49 项关键规则关键词对照两个文件 —— `MEMORY.md` 命中 43、detail 命中 41，**并集 49/49**（`MEMORY.md` 少的那 6 项：`grid-column`/`verify-weather-art`/`补记条`/`user-select`/`task-summary`/`听你的`，全在 detail 或被 `AGENTS.md` 覆盖）。
- **规矩**：以后往 `MEMORY.md` 加规则，**先看它还能不能整份注入**；细则往 detail 放，`MEMORY.md` 只留「头条 + 指针」。

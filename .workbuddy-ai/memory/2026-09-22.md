# 2026-09-22（承接 09-21 深夜那条线）

## 观影模块「让剩余流程跑通」收尾

- **做完了什么**：给观影模块补上**浏览器端到端验收**（09-21 那条只验了命令行）。
  新脚本 `.review/verify-movie-e2e.mjs`（**只读**），34 条断言：
  1. 设置页 `#settings/integrations/movie` 观影卡片：状态标签 **已连接** + `is-ready`、Key 框 placeholder **「已保存，留空不变」**、开关已勾。
  2. **在界面里点「测试连接」** → 「TMDb API 连接成功」（走 Vite `/api` 代理 ⇒ 浏览器这条通路也通）。
  3. **真识别**：首页 composer 打 `/` → 「电影」→ 面板 → 「盗梦空间」→ 识别 → 3 个候选、首位《盗梦空间》、海报 `image.tmdb.org`、首张滚动后 `naturalWidth=500`。
- **不落盘怎么保证**：只调 `GET /api/movie/status`、`POST /api/movie/config/test`、`POST /api/movie/resolve`（都不写）；**从不点「添加到记录」**（那才是 import，会建实体）。结尾用 `GET /api/records` / `GET /api/entities` 前后对比自证：149→149 / 52→52 / 电影 2→2。
- **结果**：`RESULT: PASS (34 checks)`，连跑 4 次（含冷启动）全绿。`.review/verify-movie-e2e.txt`。

## 本轮新踩到的坑（都已进 changelog / MEMORY）

1. **异步取回的状态会让「刚挂载的卡片」骗过断言**：`main.tsx:3710` 里 `movieStatus` 初值是 `{enabled:false, connected:false, …}`，真值异步到。第一版没等就断言 → 4 条假 FAIL（而同一份卡片文字 dump 里明明写着「已连接」）。**先 `waitFor` 状态落位再断言。**
2. **CDP 的 `click(expr)` 要收「求值为元素的表达式」，不是 CSS 选择器**。裸选择器塞进 `const el = ${expr}` = 页面抛语法错 → `Runtime.evaluate` 回 undefined → 报 `MISS`，而 `querySelector` 明明查得到。现在用 `$("…")` / `buttonByText("容器","文字")`。
3. **只差 hash 的 `Page.navigate` 是同文档导航** → 不能指望视图自己切回来（与 09-21 那条同源）。
4. **`reapChromeSync` 返回 true ≠ 目录真没了**：本轮早期 4 个 profile（p2rlUF / kvIuq2 / QUhzft / J9khqD）在报「已删除」的情况下留在 `.review/profiles/`。**根因没复现**（清空后连跑 3 次 + 冷启动，每次 `existsSync=false`、剩 0 个）。已手工删掉；脚本改成**自己报「运行期间新出现的 profile」+ 目录总数**，以后漏了藏不住。
5. **`node -e` 写含 `$(…)` 的文本会被 bash 当命令替换静默抹掉** —— 我在 changelog 里写 `$("选择器")` 就被吃成 `` `` ``（文件本身没坏，但少了一段）。**又违反了「先 Write 成文件再读」这条自己的规矩。** MEMORY.md 里那条已补上 `$(…)`。

## 收尾动作

- 清掉 Temp 里 10 个上一轮的一次性脚本（`lifeos-*.mjs`）与诊断探针 `.review/probe-reap-timing.mjs`。
- `.review/sweep-profiles.mjs` 双位置 **0 个 / 0.0 MB**；`audit-toolbox-targets.mjs` **0 个未守卫写入脚本**。
- `docs/changelog.md` 追加「## 2026-09-22 00:47」那条；产品代码本轮**零改动**。

---

# 2026-09-22（02:3x）主人问「9 月 17 号看《霸王别姬》没有海报」→ 查出是验收脚本污染生产库

## 结论：那条记录不是主人写的，是 `.review/verify-movie-module.mjs` 的夹具残留

三条证据：正文是脚本写死的（`今晚重看《霸王别姬》`）、对象 id `movie_verify_<时间戳>`、**海报是脚本在页内 mock 掉 TMDB 后自造的灰色 SVG 占位图**（画着「MOVIE」四个字）→ 所以「没有海报」是必然。短评写着 `验收之前的短评`。
老版本脚本**硬编码 5199/3011、通篇零个 DELETE** → 跑一次就永久往生产库写 3 条记录 + 1 个实体；2026-09-17 01:55 / 02:04 跑过两次。它躲过了 09-18 那轮守卫普查。

## 做完了什么

1. **清理**：新工具 `.review/cleanup-movie-verify-residue.mjs`（签名校验 + dry-run 默认 + 认不出一行就中止整轮）。`--apply` 后记录 2 条 **204**、实体 2 个 **204**；生产库 **149/52/2 → 147/50/0**。
2. **`purge-trash-junk.mjs` 补签名** `/^今晚重看《霸王别姬》$/`（该脚本的签名表一直漏了第三条正文，那两条墓碑永远清不掉）。dry-run `matched 7 / UNMATCHED 0`；**`--apply` 故意没跑** —— 主人选的是「可恢复」方案。
3. **`verify-movie-module.mjs` 重写成隔离 + 自清**：API 3071 / Vite 5392 / CDP 9262（可用 `LIFEOS_MOVIE_*_PORT` 覆盖），数据在 `.review/movie-module-run/`；起进程前探端口；收尾删掉自己建的记录与实体并回读复核。结果 **`RESULT: 1 FAIL (21 checks)`**，`MISS 0` / `TIMEOUT 0`，自清全 204，隔离库收尾 0 条。
4. 顺手修掉它三个潜在缺陷：① 设置页导航压根没发生（默认落 `account/session`，而 `[data-movie-settings]` 只在 `integrations/movie`）→ 显式点 `#settings-page-integrations-movie`；② `<input type="number">` 上调 `setSelectionRange` **抛异常** → 后面的 `dispatchEvent("input")` 不执行 → React onChange 永不触发 → 现在 `try/catch` + **回读校验**；③ 带判定语义的等待拆成 `waitUntil` / `waitFor`，不再一条失败计两次。崩溃处理器改到 `profile`/`chromeProc` 存在之后再注册（否则端口探测抛错会撞 TDZ）。

## 唯一那条 FAIL 已定位到行（产品缺陷，未修 → `docs/todo.md` §11）

- `main.tsx:2513` 把 `<MoviePrompt>` 塞进 `<p className="timeline-text">` **里面**；`movie.tsx:307` 的 `MoviePrompt` → `movie-prompt-popover` → `MovieAddPanel`，而后者的根是 **`<div className="movie-add-panel">`**（`movie.tsx:262`），里面有 **`<form>`**（:265）与 **`<p>`**（:266/:268）→ `<div>`/`<form>`/`<p>` 全成了 `<p>` 的后代。
- **为什么恰好 6 条**：React 按 `!!parentInfo + "|" + childTag + "|" + ancestorTag` **去重**（`react-dom-client.development.js:2759-2761`）→ 3 组合 × 2 条 = 6。
- 另一挂载点 `main.tsx:1049`（composer 的 `/电影`）外面是 `<div>` 链 → 不触发；5199 上的只读探针专走那条路，**4 个阶段 0 条 error**。
- 触发条件 `main.tsx:2507`：模块开着 + 正文含「电影」的日记 + 还没挂电影 → **主人的真实路径会踩**。

## 本轮新踩的坑

1. **React DOM 嵌套警告的祖先栈里，不带 props 的裸标签是「兄弟节点」，不是祖先**（`describeNode` → `describeSiblingFiber`，`react-dom-client.development.js:2486-2497`）。我第一眼把 `main.main-column > header` 当成链上节点，得出「警告在 topbar」的错误结论；实际 `header` 只是 `div.content-grid` 的**前一个兄弟**，链走的是时间线列。**读这类栈先分清「带 props 的」和「裸标签的」。**
2. **我自己的捕获代码把证据切了**：`.slice(0, 200)`，而这条消息到 `<header>` **正好 200 字符** —— 截断点太整齐，反而让「栈就到这里」看起来像事实。**诊断用捕获别设这种量级的上限。**
3. **`.review/audit-toolbox-targets.mjs` 靠 `/method:\s*["'](POST|PATCH|PUT|DELETE)["']/` 认「会写的脚本」** → 写 `send(path, "DELETE", body)` 会被归成「只读」。必须写成 `{ method: "DELETE", body }` 的字面量形状。

## 收尾

- `docs/changelog.md` 追加「## 2026-09-22 02:35」；`docs/todo.md` 新增 §11 并更新「最后更新」行。**产品代码本轮零改动**（只动 `.review/` 工具与 docs）。
- 删掉一次性探针 `.review/scan-movie-residue.mjs`。
- `audit-toolbox-targets.mjs`：`isolated 40 / will-write 19（guarded 15 + prod-tool 4 + UNGUARDED 0）`，退出码 0。

---

# 2026-09-22（02:52）主人选 D 路线 → 那 6 条非法嵌套修掉了（一行）

## 改了什么

**`apps/web/src/main.tsx:2513`**：`<p className="timeline-text">` → **`<div className="timeline-text">`**（闭合标签一起改）。**产品代码只动这一处。**

为什么一行就够（逐项核对过，不是推断）：样式与 `.review/` 里 12 个脚本**全按类选择器**挂钩（`styles.css:182/1960/6930`、`verify-selectability.mjs:44`）；唯二受标签影响的 `p { margin-top: 0 }`（`styles.css:1357`）与 UA 默认 margin **都被 `.timeline-text` 自己的 `margin: 9px 0 0` 覆盖**；`p` 的后代选择器都不在 `.timeline-text` 子树内；弹层锚在 `.movie-prompt-wrap`（自己 `position:relative`），不受影响。

## 验证数字

- `npm run typecheck` → core / api / web **三段全绿**。
- `node .review/verify-movie-module.mjs` → **`RESULT: PASS (21 checks)`**，`PASS zero console errors (0)`（原 `FAIL … (6)`），连跑 4 轮都 21/21。
- `node .review/verify-selectability.mjs` → **`RESULT: PASS (59 checks)`**，`.timeline-text` 四条断言全 ok（`user-select: text` + 拖蓝选出「看复联5」）。

## 顺带修掉的两处脚本收尾

1. **`child.kill("SIGKILL")` 在 Windows 上不保证进程已死** → `rmSync(RUN)` 一直失败（API 开着 sqlite，API/Vite 还继承了 `api.log`/`web.log` 句柄）。改成对子进程也走**同步 `taskkill /F /T /PID`**（新助手 `killTreeSync`）→ `movie-module-run` 再没留下过。
2. **profile 泄漏**：`reapChromeSync` 报成功、目录却会回来，**每轮漏约 50MB**（清掉 6 个共 **250.6MB**）。缓解：`sweepProfiles` 窗口 6h → **15min**；收尾「盯 3 秒、回来就再杀再删」；**自己列一遍 profile 目录再报数**。**根因未查清** → `docs/todo.md` §12。

## 这条 profile 之谜的实测结论（都写进 §12 了）

- 删除**确实发生**（连续 2.75s `exists=false`）；几秒后目录**又完整长回来**（115 目录 / 278 文件）；事后 `chrome.exe` = 0、`chrome_crashpad_handler.exe` = 0；同一目录稍后普通 `rmSync` 一次就删掉；3 秒的「盯住再删」抓不到（`reappeared=0`，但最终列表里仍是它）。
- **已否掉的假设**：「`spawn` 拿到的是启动器 pid、`taskkill` 打空」。探针实测 `spawn` 返回的 pid 就是主浏览器进程（10 个 chrome 进程都指向它）。探针文件跑完已删。
- **还没测到的**：目录重新出现的那一刻到底有没有 chrome 进程 —— 脚本内 `execFileSync("tasklist", …)` 抛错（`chrome=-1`），**抛的什么错没查**（外面手工调 tasklist 是好的）。这是下一步最该补的数。

## 收尾

- `docs/changelog.md` 追加「## 2026-09-22 02:52」；`docs/todo.md` §11 标 **已修**（保留原始记录）+ 新增 §12 + 更新「最后更新」行。
- `.review/profiles/` 清空（250.6MB）；一次性探针 `.review/_probe-chrome-pid.mjs` 已删。
- `MEMORY.md` 去重压缩：7892 → **6495 字符**（bytes 13783 → 11377），24 项关键规则抽查全部在位。

## 再压一轮：`MEMORY.md` 会**整份被截断**，尾部三节等于没有（03:0x）

- **触发**：本轮进场时系统提示 `MEMORY.md` 超限、注入时被截断 —— 截断点正好落在 `## UI 硬规则` 的「卡片宽度」那条，**后面三节（UI 硬规则 / 日历）根本没进上下文**，而且**连个指针都没有**。上一轮压到 6495 字符仍然超。
- **处置（不是删规则，是搬家）**：把 `## 时光机` / `## UI 硬规则` / `## 日历` 三节**逐字**移进 `MEMORY-detail.md` 新增的 `## 细则全文（2026-09-22 从 MEMORY.md 移入…）`；`MEMORY.md` 尾部换成一块**指针 + 每节头条规则**（含命令与阈值），并改文件头明说「本文件必须能整份注入」。
- **数字**：`MEMORY.md` 6749 → **5107 字符**（bytes 11834 → **8532**，−28%）；detail 36720 → 39496 字符。
- **校验**：49 项关键规则关键词对照两个文件 —— `MEMORY.md` 命中 43、detail 命中 41，**并集 49/49**（`MEMORY.md` 少的那 6 项：`grid-column`/`verify-weather-art`/`补记条`/`user-select`/`task-summary`/`听你的`，全在 detail 或被 `AGENTS.md` 覆盖）。
- **规矩**：以后往 `MEMORY.md` 加规则，**先看它还能不能整份注入**；细则往 detail 放，`MEMORY.md` 只留「头条 + 指针」。

## 任务定时改成范围选择器（07:45）

- **主人原话**：「任务定时 跨月份选择月份很麻烦要先点月份然后展开 再选下月…任务开始也不能定未来…应该开始和结束都能是未来 并且可以在小小月历上点开始和结束 然后就选定了范围 目前选择器的月历很丑，你先做 不用问我 做好了我看就行了 就按你理解」→ **明确授权「不用问」**，所以做完再汇报。
- **交付**：新增 `apps/web/src/task-schedule.tsx`（`TaskScheduleField`，约 330 行）：**常驻翻月箭头**（一次点击翻一月）、**两端都不封顶**、**点两天成范围**（连续色带 / 端点实心 / 今天内描边环）、四个快捷（含「一个月后」）、面板**先量两侧余量、往大的那边开**。`main.tsx` 只换 2 行；`styles.css` **纯新增**约 330 行（被删/改写非空行 **0**，行级差集核对）。
- **验收**：`.review/verify-task-schedule.mjs` → **PASS (39 checks)**（隔离实例 API 3073 / Vite 5394，落库按 Asia/Shanghai 对天、自清 204）；`typecheck` 三段绿；`npm test` **57/57**；回归面 `verify-composer-date` 113/114、`verify-selectability` 59、`verify-mobile-layout` PASS、`verify-mobile-composer-rail` PASS、`audit-toolbox-targets` UNGUARDED 0。
- **坑**：
  1. 浮层向上开被 `max-height` 压成**两行**（`.task-cal` 67px）→ 第二次点击落到时间列上；**CDP 点错元素不报错**，靠 `elementFromPoint` 命中测试才看见。修法在产品里（量两侧、往大的开）。
  2. 记录正文在 **`body.original`**（没有 `content` 字段）；`occurredAt` 是 UTC 瞬时 → 按天核对必须走 `Asia/Shanghai`（差点第三次踩）。
  3. **我自己的收尾写反了**：拿 `existsSync` 当「要不要删」的前置判断，而它把任何 `stat` 错误吞成 `false` ⇒ 删除整段被跳过、脚本却报「已删」（`-0BPBss` 一直在盘上）。改成**无条件先删** + 用**父目录 `readdirSync`** 当见证。
- **顺手修的三处过期判据**：`verify-movie-module.mjs` 少 import `execFileSync` ⇒ `killTreeSync` 从没生效 —— **这就是 `docs/todo.md` §12 里那个 `chrome=-1` 之谜的答案**；`verify-mobile-composer-rail.mjs` 还在量自 `df9b6f3` 起就不存在的 `.weather-date-context small`（每次必红 + 白等 12 秒）；同一脚本裸 `SIGTERM` + 裸 catch 的 `rmSync` 已攒 4 个运行目录。`killTreeSync` 挪进 `.review/reap-chrome.mjs` 作导出。
- **清理**：`.review/profiles` 清 6 个 / **303.8MB**，随后又清 2 个 / **64.7MB**；`mobile-composer-rail-run-*` 清零（连跑两轮、外部核对 = 0）。
- **留白**：`verify-composer-date` 的 `the panel fits the phone`（`left=154 right=474 viewport=430`）是**产品老缺陷、改动前就红**，记 `docs/todo.md` §13.1 等主人拍板；另有 **1 个无法归因**的 rail 运行目录（birth `23:32:03Z`，含真实 API 日志与 chrome tmp，但当时我只在跑 `npm test`）记 §13.3，**未证实、不写成结论**。
- **文档**：`docs/changelog.md` 追加 07:45 条（带 `AI:` 署名）；`docs/todo.md` 新增 §13 + 更新「最后更新」+ 附录加两行工具。
- **记忆**：`MEMORY.md` 并入两条（`existsSync` 吞错 / 浮层量两侧 + 别对浮层内容 `scrollIntoView`），并顺手压缩重复表述 → **5496 字符**（截断发生在 6749 字符那个版本，留有余量）。

## 收尾自查补上第二个挂载点：编辑弹窗的开始日也解封（08:10）

- **怎么发现的**：按 `dueAt` / `onDueAtChange` 全仓搜了一遍，发现同一件事有两个挂载点。`main.tsx:2787` 的 `RecordEditor`（点卡片「编辑记录」开的 `<dialog class="editor-dialog">`）里，任务的「发生时间」**仍然带 `capped`** ⇒ composer 里设得出来的「一个月后触发」的任务，**一进弹窗就改不动了** —— 主人抱怨的原话正是这条，所以算把要求补完。
- **改动**：产品代码只动一个属性 —— `capped` → **`capped={task === undefined}`**（`task = isTaskRecord(record) ? record : undefined`，`main.tsx:2786`）；非任务记录仍封顶（日志记的是已经发生的事）。
- **验收**：`.review/verify-task-schedule.mjs` 新增 8b 段，**39 → 47 项全 PASS**：建一条 `kind:"task"` + 一条 `kind:"journal"`（对照），点 `button[aria-label="编辑记录"]` 开弹窗 → 展开面板与月历 → 读 `data-date-capped` 与 `.date-grid-day[disabled]` 个数。实测 **任务 `capped=off` / 被锁 `0/42` 格**、**对照 `capped=on` / 被锁 `19/42` 格**；三条自清全 204；console error 0。`typecheck` 三段绿。
- **坑（值得记）**：第一版对照判据读的是「下个月」箭头的 `disabled`（`capped && isFutureMonth(shiftMonth(monthAnchor,1))`），结果对照那条 `capped=on` 却 `disabled=false`。根因：对照记录的 `occurredAt` 是**空的**（`POST /api/records` 只给 `kind`/`content`，API 收了、日期留空）⇒ `monthAnchor = ""` ⇒ `shiftMonth("",1)` 是垃圾值 ⇒ `isFutureMonth` 正则不匹配、返回 false。**判据挂在机制的副产物上就会这样假红**；改成数被锁格子（与锚点无关）后干净通过。已写进 `MEMORY.md`。
- **没做的（`docs/todo.md` §14，等主人拍板）**：弹窗里仍是两只单日选择器（无范围色带、翻月仍要「先展开」）。拦路石是 `.dialog-body { max-height: 57vh; overflow-y: auto }`（`styles.css:6765-6771`）会裁掉 `position: absolute` 的面板；要做对得给面板加 `position: fixed` 模式 + 滚动重算，属组件能力扩展，不是顺手能带的。
- **清理**：`.review/profiles` 又清 2 个 / **133.1MB**（每轮仍漏 1 个，§13.2）。

# 2026-09-22（08:25）§12 结案：profile「删不掉」的真因是**沙箱的删除闸门**，不是 Windows 句柄

> **本节取代上面 02:52 那条「## 这条 profile 之谜的实测结论」**（日志 append-only，所以不改旧文，在此更正）。
> 那一节写的「删除确实发生 → 几秒后又完整长回来」**是误读**：**删除从来没有发生过**。

## 一句话

`reapChromeSync` 报成功、profile 却还在 —— 因为**沙箱自己的 `safe-delete` 闸门**在删任何东西之前就把它拒了，而收尾的裸 `catch {}` 把拒信吞了。**跟 Windows 句柄、启动器 pid、长路径都无关**；那些方向我查了一整天。

## 证据链（三条，缺一条就还在猜）

- **`renameSync(profile, moved)` 成功** —— Windows 上任一句柄占用都会让改名失败 ⇒ **没有进程攥着它**。
- **`chrome.exe` = 0**，每个采样点都是。
- **闸门自己的状态目录** `%TEMP%\codebuddy-safe-delete-bulk\<sessionHash>\`：6 条 `confirmRequired`，目标正是本会话那些我以为「已清掉」的目录 —— `…-reap-forensics-cE4WmW-moved`(337)、两个 `lifeos-cdp-task-schedule-*`(1302 / 1307)、`data/derived/snapshots/lifeos-20260918-215809-08a75403.sqlite`(7220)、`.review/mobile-composer-rail-run-9Iwo8Y`(16095)、`.review/movie-module-run`(9150)。`scope:"turn"`、`threshold:50`。
- 抛出的错：`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {…}`，且 **`code`/`errno`/`syscall`/`path` 全是 `undefined`** → 旧代码印成 `(undefined)`，把方向带偏。

## 改了什么（**不绕过闸门，只是不再说谎**）

- 新增 `.review/lib/delete-guard.mjs`（判定 + 措辞，**永不打印 `undefined`**）；`reapChromeSync`/`reapChrome` 撞上闸门**立刻停**（重试无用），`sweepProfiles`/`sweepStaleProfiles` 返回值带 `blocked[]` + WARN；`sweep-profiles.mjs` 新增 `RESULT: BLOCKED` 结论（退出码 1）。
- 新增 `.review/verify-delete-guard.mjs` → **PASS (13 checks)**，落盘 `.review/verify-delete-guard.txt`。
- `docs/todo.md` §12 重写 + §13.2 结案；`docs/changelog.md` 追加 08:20 那条（带 `AI:` 署名）；`MEMORY-detail.md` 新增详解一节。

## 仍然未知的一半（**别猜，也别写成结论**）

沙箱被绕过的调用（工具结果里打印 `Sandbox bypassed (escalation-approved)`，环境里**没有** `CODEBUDDY_SAFE_DELETE_SANDBOX`）删除不设闸、直接成功。但**哪一次调用会被批准 / 被绕过是宿主侧的决定**，进程内看不到；状态里有 44 条 `approved`、累计计数高到 `27273`，说明「批准」这条路是通的 —— **具体某一次为什么走通，没有证据**。

## 数字

- `typecheck` 三段绿；`npm test` **85 / 85**（core 28 + api 57）—— **产品代码一行未动**。
- `.review/profiles` 清空（本轮共回收 **303.8MB + 64.7MB + 11.9MB**）；一次性探针（`_probe-reap-forensics.mjs`、`_probe-gate-contract.mjs`）与拼接用的临时文件（`_splice12.mjs`、`_new-*.md`）全部删掉。
- `MEMORY.md` 去重 **5779 → 5300 字符**（与 `MEMORY-detail.md` 重复的时光机 / UI / 日历三节收敛成红线，规则一条没丢）。

## 坑

- **`npm test` 报 `node: bad option: --test-isolation=none`** 时不是测试坏了：PATH 上解析到的是 managed 的 Node 22.x。要 `export PATH="/c/Program Files/nodejs:$PATH"`（24.x）再跑。
- 本轮注入进上下文的 `MEMORY.md` 是**旧版本**（里面有「判别看元…」这种当前文件里根本不存在的句子），所以那条「超出大小限制被截断」的告警**至少有一部分来自缓存**，不能直接当成对当前文件的判断。

# 2026-09-22（08:25）§12 结案：profile「删不掉」的真因是**沙箱的删除闸门**，不是 Windows 句柄

> **本节取代上面 02:52 那条「## 这条 profile 之谜的实测结论」**（日志 append-only，所以不改旧文，在此更正）。
> 那一节写的「删除确实发生 → 几秒后又完整长回来」**是误读**：**删除从来没有发生过**。

## 一句话

`reapChromeSync` 报成功、profile 却还在 —— 因为**沙箱自己的 `safe-delete` 闸门**在删任何东西之前就把它拒了，而收尾的裸 `catch {}` 把拒信吞了。**跟 Windows 句柄、启动器 pid、长路径都无关**；那些方向我查了一整天。

## 证据链（三条，缺一条就还在猜）

- **`renameSync(profile, moved)` 成功** —— Windows 上任一句柄占用都会让改名失败 ⇒ **没有进程攥着它**。
- **`chrome.exe` = 0**，每个采样点都是。
- **闸门自己的状态目录** `%TEMP%\codebuddy-safe-delete-bulk\<sessionHash>\`：6 条 `confirmRequired`，目标正是本会话那些我以为「已清掉」的目录 —— `…-reap-forensics-cE4WmW-moved`(337)、两个 `lifeos-cdp-task-schedule-*`(1302 / 1307)、`data/derived/snapshots/lifeos-20260918-215809-08a75403.sqlite`(7220)、`.review/mobile-composer-rail-run-9Iwo8Y`(16095)、`.review/movie-module-run`(9150)。`scope:"turn"`、`threshold:50`。
- 抛出的错：`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {…}`，且 **`code`/`errno`/`syscall`/`path` 全是 `undefined`** → 旧代码印成 `(undefined)`，把方向带偏。

## 改了什么（**不绕过闸门，只是不再说谎**）

- 新增 `.review/lib/delete-guard.mjs`（判定 + 措辞，**永不打印 `undefined`**）；`reapChromeSync`/`reapChrome` 撞上闸门**立刻停**（重试无用），`sweepProfiles`/`sweepStaleProfiles` 返回值带 `blocked[]` + WARN；`sweep-profiles.mjs` 新增 `RESULT: BLOCKED` 结论（退出码 1）。
- 新增 `.review/verify-delete-guard.mjs` → **PASS (13 checks)**，落盘 `.review/verify-delete-guard.txt`。
- `docs/todo.md` §12 重写 + §13.2 结案；`docs/changelog.md` 追加 08:20 那条（带 `AI:` 署名）；`MEMORY-detail.md` 新增详解一节。

## 仍然未知的一半（**别猜，也别写成结论**）

沙箱被绕过的调用（工具结果里打印 `Sandbox bypassed (escalation-approved)`，环境里**没有** `CODEBUDDY_SAFE_DELETE_SANDBOX`）删除不设闸、直接成功。但**哪一次调用会被批准 / 被绕过是宿主侧的决定**，进程内看不到；状态里有 44 条 `approved`、累计计数高到 `27273`，说明「批准」这条路是通的 —— **具体某一次为什么走通，没有证据**。

## 数字

- `typecheck` 三段绿；`npm test` **85 / 85**（core 28 + api 57）—— **产品代码一行未动**。
- `.review/profiles` 清空（本轮共回收 **303.8MB + 64.7MB + 11.9MB**）；一次性探针（`_probe-reap-forensics.mjs`、`_probe-gate-contract.mjs`）与拼接用的临时文件（`_splice12.mjs`、`_new-*.md`）全部删掉。
- `MEMORY.md` 去重 **5779 → 5300 字符**（与 `MEMORY-detail.md` 重复的时光机 / UI / 日历三节收敛成红线，规则一条没丢）。

## 坑

- **`npm test` 报 `node: bad option: --test-isolation=none`** 时不是测试坏了：PATH 上解析到的是 managed 的 Node 22.x。要 `export PATH="/c/Program Files/nodejs:$PATH"`（24.x）再跑。
- 本轮注入进上下文的 `MEMORY.md` 是**旧版本**（里面有「判别看元…」这种当前文件里根本不存在的句子），所以那条「超出大小限制被截断」的告警**至少有一部分来自缓存**，不能直接当成对当前文件的判断。

# 2026-09-22（14:58）主人问「观影模块怎么验收 / 怎么知道生效 / 怎么用起来」→ 三层验收全跑了一遍

## 三个问题的答案（都给了可复现命令，不是复述文档）

1. **怎么用**：设置 → 服务集成 → 观影（`#settings/integrations/movie`）开开关 + 填 key；首页 composer 打 `/` → 「电影」→ 输片名/链接/ID → 识别 → **候选必须手点** → 添加到记录；记录上的电影胶囊点开填评分（0–10、0.5 步长）与短评。**没有独立观影列表页** —— 全仓搜 `观影` 只有设置页那一处。
2. **怎么验收**：三层 —— 只读冒烟 12 项 / 隔离 UI 契约 21 项 / 贴预览端到端 34 项。
3. **怎么算生效**：状态标签「已连接」（`movie.tsx:217` 的四档：已关闭 / 已连接 / 待测试 / 未配置）+ composer 打 `/` 菜单里有「电影」+ 真识别出候选；硬证据是 `GET /api/movie/status` 的 `configured/hasKey`。

## 本轮实测数字（刚跑的，不是旧结论）

- **新写 `.review/probe-movie-live.mjs`（只读，12 项）→ PASS**。实测：`movie/status` = `{"enabled":true,"configured":true,"hasKey":true,"source":"file"}`；`config/test` → 200「TMDb API 连接成功」；`resolve {query:"盗梦空间"}` → 3 个候选、首个就是「盗梦空间」、海报域名 `image.tmdb.org`。**并自证没写**：`movie-config.json` 168B / sha256 / mtime 前后完全一致，records 150→150、entities 50→50。
- `.review/verify-movie-module.mjs` → **PASS (21 checks)**；隔离实例（API 3071 / Vite 5392 / CDP 9262）自清后记录 0 / 实体 0。
- `.review/verify-movie-e2e.mjs` → **PASS (34 checks)**。**它被生产守卫拦了**（贴 3011/5199），按守卫自己给的出口用 `LIFEOS_ALLOW_PROD_ACCEPTANCE=1` 放行；脚本从不点「添加到记录」，并以 150/50 前后一致自证只读。
- 收尾：`.review/profiles` 清 2 个 / **94.2 MB** → `RESULT: CLEAN`（新的闸门判定没有误报）。

## 记进哪了

- `docs/todo.md` 工具表 **+3 行**：三层验收各自「碰不碰生产库」、以及 e2e **需要显式放行**。
- `MEMORY-detail.md` 新增一节「观影模块：怎么用、怎么验、怎么算生效了」（四步用法 / 状态标签四档 / 三个易误判点）。
- `MEMORY.md` 只加一行（三层验收 + 放行口），其余细则留在 detail。

## 顺带确认的两件事

- 主人库里现在 **movie 实体 = 0**（09-21 那次是 2）⇒ 观影是「配好了但还没开始用」的状态。
- **`verify-movie-e2e.mjs` 需要 `LIFEOS_ALLOW_PROD_ACCEPTANCE=1` 这件事以前没写进任何文档** —— 下一个 AI 会以为脚本坏了。这次补上了（todo 工具表 + MEMORY.md）。
- 「测试连接」不落盘这次又实测了一遍（sha256 + mtime 都不动）⇒ **测试成功 ≠ 已保存**，填了新 key 还得点「保存配置」。

# 2026-09-17

## 00:50 · 补记条（安慰剂）+ 展开/折叠动效 + 保存按钮改名

**主人三条**：① 补记条条到展开要有丝滑动效 ② 条条上也要有「日记/任务/事件/笔记」四个标签，标签右边是输入框，最右边是补记按钮（复用保存记录按钮样式）③「保存记录」按钮改叫「保存」。

**主人对「补记」按钮的定义（原话，务必记住）**：
> 「应该是一个安慰剂按钮，点刚才我说的任意地方都是展开，补记也会随着展开变成保存，只是昭示目前不是今天、处于一个补记状态而已。」

→ **整条一个点击目标**；标签和按钮都是 `aria-hidden` 的 span；`补记` 和 `保存` 是**同一动作的两个阶段**。

### 交付

- `main.tsx` 的 `ReviewComposer` 重写：折叠态 `.review-composer-bar`（一个真 `<button>`）内含 4 个 kind 标签（当前类型高亮）、一个草稿预览 span（无草稿显示 placeholder）、一个 `.primary-button` 写的「补记」。
- 展开：slot 高度 JS 驱动（0 → 实测 scrollHeight → 交还 `auto` + 撤 `overflow: hidden`）。
- 折叠：**先钉住当前像素高度一帧再设 0**。
- 编辑器保存按钮文案 → 「保存」。
- **顺手修**：`dismissible: isReviewingPast || !isToday`（原来 `!isToday` 在「今天」视图恒 false → 展开后回不去，条条成单向门）。

### 验收 40/40 PASS（`.review/review-mode.txt`）

- 折叠态：`barTag BUTTON`、标签 `日记/任务/事件/笔记`、`barActiveKinds 1`、`barCta 补记`、`barCtaIsPrimary true`、**`barNestedControls 0`**、`editorMounted false`、`slotHeight 0`。
- 展开逐帧：`[0,0,10,51,90,172,226,258,269,283,290,292…]` → 11 个不同高度。
- 折叠逐帧：`[292,292,292,282,241,158,119,65,34,15,9,2,0…]` → 11 个不同高度。
- 折回后草稿原样保留、条条预览显示草稿（`has-draft`）。
- typecheck 绿、build 通过、npm test 47/47；五个回归脚本 PASS。

### 新学到的

1. **`height: auto` 不能参与 transition**：折叠时直接从 auto 设 0 会瞬移（第一版实测 `[292, 0, 0, …]`）。必须先读 `getBoundingClientRect().height` 钉一帧再设目标值。展开方向本来就正常，因为起点恰好是显式 `0`。
2. **编辑器容器不能长期 `overflow: hidden`**：日期面板与提及列表是绝对定位挂在里面的。裁切只在过渡期间存在，结束必须交还 `visible`。
3. **`dismissible: !isToday` 的语义坑**：`isToday` 是「当前视图是今天视图」，不是「日期是今天」。名字看着对、含义差一层。
4. **验收脚本的日期夹具不能写死（本轮真踩）**：`verify-weather-observe.mjs` 写死 `2026-09-16`/`2026-09-17`，**一过午夜就失效**，3 条断言红，看起来像产品回归。已改成 `Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Shanghai" })` 动态推导。**`verify-weather-device.mjs` 有同样问题（硬编码 09-14/09-15 且断言翻到 09-15），未修；`verify-weather.mjs` 也硬编码但靠 `snapshot.today` 兜底仍 PASS（绿得侥幸）。**

### 遗留

- 条条上的 4 个标签是纯指示，点了也是展开、不切类型（主人明确要「安慰剂」）。
- 保存成功后编辑器不自动折回条条。
- 上面第 4 条里两个未修的脚本。

---

## 01:20 · 补记条与正式版几何对齐 + 折叠竞态修复 + 新规则「不出截图」

**主人两条**：① 补记条的几个按钮（含保存按钮）跟正式版**x 轴位置、体积都一样** ② **每次做完不用出截图，主人直接看预览端口，写进 AGENTS.md**。

### 交付

- **`AGENTS.md` 铁律第 6 条**扩写：不要主动生成截图、不要在回复里贴图，主人自己开预览端口；收尾只汇报改动/验证/数字/预览地址。（AGENTS.md 被 gitignore，只在本机磁盘。）
- 补记条的类型标签改用**编辑器自己的 class**（`kind-switcher` + `kind-option`），主按钮用 `primary-button` + 补 `Send` 图标（不补图标宽度就差一个图标）。
- 条条水平内边距 `20px`（= `.composer` 的左右 padding）、圆角 `21px`（= `.surface`）。

### 验收 45/45 PASS

- 塌缩态条条 4 个标签 `[[372.5,72.3,36],[450.8,72.3,36],[529,72.3,36],[607.3,72.3,36]]` 与展开态编辑器**完全相同**；主按钮 `[右缘 1110.5, 宽 81.9, 高 44]` 也相同。
- 展开 13 个不同高度、折叠 11 个不同高度。
- typecheck 绿、build 通过、npm test 47/47、五个回归 PASS。

### 新学到的（都值得刻死）

1. **折叠动效第一版只是碰巧通过**：用 React state 写「钉住当前高度 → 下一帧设 0」会被**批处理进同一帧**，浏览器只看到 `auto → 0` 一次变化 → 不触发 transition。修法：`useLayoutEffect` + **直接写 `slot.style.height`** + 两次写入之间 `void slot.offsetHeight` 强制回流。`useLayoutEffect` 还保证首帧就是 0，不会先全尺寸闪一下。
2. **同名 class 覆盖要带父级前缀**：`.kind-switcher` 自带 `margin: 22px 0 12px`，把我 `.review-composer-kinds { margin: 0 }` 顶掉（同优先级、它在后面）→ 条条被撑到 88px、标签下沉。改成 `.review-composer-bar .review-composer-kinds` 才生效（62px）。
3. **几何对比要在「主人实际看到的状态」下量**：展开态 CSS 会把条条 `border-width` 设成 0，于是少 1px → 报出 `371.5` vs `372.5` 的假差异。**塌缩态量条条、展开态量编辑器**才对。（那 1px 正是编辑器 `.surface` 自己的边框。）
4. **验收脚本崩溃会留下上一次的 `RESULT: PASS`**：我笔误写了不存在的 `checkTrue`，脚本抛错退出，结果文件里仍是 `RESULT: PASS (31 checks)`。已给 `verify-review-mode.mjs` 加 `uncaughtException` / `unhandledRejection` → 任何未捕获错误都改写成 `CRASH …` 并退出 1。**其它 verify 脚本还没有这层保护。**

### 遗留

- 其余 `.review/verify-*.mjs` 没有「崩溃即 FAIL」保护。
- 标签仍是纯指示；保存后不自动折回条条；`verify-weather-device.mjs` 写死日期未修。

---

## 01:30 · 写了三轮递进式代码审计提示词

**主人要**：一个让 AI 逐轮查 bug、每轮举一反三扩大搜索、三轮后逐条修复的提示词，**要针对本项目**。

### 交付

- **`docs/audit-prompt.md`**（新建）：完整提示词，新开会话整段贴进去即可用。
- **`AGENTS.md`** 新增「深度审计提示词」小节指向它，并写明**新增踩坑类别时要追加进该文件第 1 轮的模式库**（那是弹药，不追加就退化）。

### 提示词的设计要点

1. **递进机制是核心**：每轮必须产出两类东西 —— **实例**（文件:行 + 症状 + 证据）和**类别**（一句话命名 + 可复用搜索模式）。**下一轮的开场动作 = 执行上一轮所有类别的搜索**，再补新类别。
2. **三轮的搜索轴逐轮加宽**：
   - 第 1 轮：**模式库扫捕** —— 把本项目真实踩过的坑（时间时区 / CSS 布局动画 / 状态并发 / 验收基建四组）当模式库，全仓找**还没被发现的同族**。
   - 第 2 轮：**按轴扩张** —— 时间、布局渲染、状态契约、失败路径四条轴，找形态不同但根因同族的。
   - 第 3 轮：**跨层与边界** —— 导出导入往返、空与极值、性能预算回归、可访问性、性能。
3. **模式库是项目专属的**，写进去的都是真事：UTC `slice(0,10)` 归错天、42 格月历首格、硬编码日期夹具过午夜失效、`animation-fill-mode: none` 让元素停在基准位、`height: auto` 不可插值、同优先级 class 后写者赢、`transform` 不改变布局盒、reduced-motion 停在最后一帧、动画属性白名单、`backdrop-filter` 压动画层、细周期图案高速平移出摩尔纹、内联 `!important` 打死选择器、乐观锁 revision、React 批处理吃掉中间帧、缓存键缺 `targetDate`、断言测 DOM 存在而非视觉可见、瞬态缺陷只比前后、脚本崩溃留假 PASS、缺省端口 3012 等。
4. **明确区分审计与修复阶段**：三轮跑完前**一个字节都不许改源码**；修复阶段一次一件、每个都带验证、断言要按「意图」写而不是按「当前实现恰好量到的数字」写、修不动的必须标出来。
5. **汇报格式**给了固定模板（≤15 行/轮），并要求最终总表含状态与提交号。
6. 把环境铁律也编进去了（Node 24 直调 npm、bash 残缺、`node -e` 反引号陷阱、不许杀 3001、禁止 `reset --hard`、不出截图）。

---

## 13:00 · 天气「允许改位置」三级下拉 — 只读勘察 + 草稿（**未动任何现有文件**）

**主人要求**：设置→天气加「允许改位置」，用**省/市/区三级下拉**，背后映射和风 Location ID，**不能让用户手输城市然后搜不到**。因为另一个 AI 正在改别的东西 → **本轮禁止改动现有文件**，先写临时草稿。

**主人当场拍板的三个决定**（AskUserQuestion）：
1. 数据来源 = **离线内置全量表**（不是实时调 GeoAPI）。
2. 落盘位置 = **`.review/weather-location-draft/`**（隔离草稿区）。
3. 区县粒度 = **必须选到「市」，区县选填**（市级 ID 完全能出天气，全国 2800+ 区县不该强制）。

### 交付（全部在 `.review/weather-location-draft/`，git status 证明源码目录零改动）

- `China-City-List-latest.csv` — 和风官方城市列表（`github.com/qwd/LocationList`，v202506200，472 KB）
- `compile-locations.mjs` → `weather-locations.data.json`：**34 省 / 392 市 / 3572 位置 / 155 KB**
- `weather-locations.ts`（离线查找）+ `weather-location-types.ts` + `WeatherLocationPicker.tsx`（三级下拉组件）
- `verify-data.mjs`（33 断言）+ `verify-lookup.mjs`（30 断言，含**全量 3572 往返**）
- `INTEGRATION-PLAN.md` — 给下一个 AI 的集成清单（含 `main.tsx` 的精确 diff）

**验收：data 33/33 PASS、lookup 30/30 PASS、tsc strict 零错误**，落盘 `.review/weather-locations-draft.txt`。

### 本轮踩到的坑（真踩，都记进 INTEGRATION-PLAN.md §6）

1. **城市自己那一行的名字 ≠ `adm2`**：源表写 `哈尔滨` 而 `adm2` 写 `哈尔滨市`（`北京` vs `北京市` 同）。用 `name === adm2` 判断城市行**全部不成立** → 城市行被当区县塞回 → **377 个 ID 同时出现在两级**。修法：比较前剥行政后缀。**只有「无重复 ID」断言抓住了它，肉眼抽查全会通过。**
2. **自治州/省直辖县没有「全域」那一行**：延边州的行是 `延吉/敦化/…` + 一行 `延边`，**第一个是州府不是州**。为「别留空 option」把 `districts[0].id` 提上来当市级 → 那个 ID 又出现在两级。修法：优先找名字等于州名的行；找不到才用州府并**从区级移除** + 打 `seatOnly` 标记。
3. **CSV 含引号字段**：台湾行国家名是 `"Taiwan, Province of China"`（**含逗号**），`split(",")` 会让后面**整体右移一格**，`adm1/adm2` 全错。**只解析前几行看不出来**（北京那几行没引号），是「34 个省级单位」断言暴露的。修法：正经 RFC 4180 逐字符解析。
4. **验收脚本别手写 TS 擦除**：项目里就有 `typescript`，用 `ts.transpileModule`。另外 **`data:` URL 无法解析相对 import**（Node: `Invalid relative URL or base scheme is not hierarchical`）→ 要落真实临时文件，且**相对 import 的 JSON 也要一起拷**（否则 `ERR_MODULE_NOT_FOUND`）。

### 关键设计点（集成时别改坏）

- `WeatherLocationPicker` **完全没有自由文本搜索框** —— 这是「搜不到」在**结构上**不可能发生的保证，不是靠体验优化。
- 换上级必须清空下级（否则切到北京还留着南海，ID 会静默错省）。
- **反查不到的 ID（境外/旧配置）不能丢**：集成方案里保留了收起的「手动输入 Location ID」逃生口，默认折叠，避免重新出现手输框。
- **港澳台 ID 是非数字的**（香港 `101320101`、油尖旺 `14606`），已正常收录，但**未实测 devapi 能否出天气**（需要主人的 Key）。



## 16:30 · 勘察 opencode（union-alpha）那批改动的破坏面（只读，未动手）

主人给了一个 opncd.ai 分享链接，说「另一款 AI 好像改坏了项目，改到一半改不下去」。任务：只评估，不动手。

**结论：工作区里压着未提交改动，git 最后一次提交是 c6207a7（09-17 08:33）**，opencode 那批**全部没提交**。

### 1. 已提交的基准（好版本所在）
- commit 0c25baa（09-16 23:44）「height-balanced two-column history」= 回看双列 + SVG 回环连线 + 卡片样式。
- 提交历史只到双列**本身**；之后 opencode 的改动全在 working tree。

### 2. 未提交改动 A：回看双列的「卡片化」被整体拆掉（主人说的坏事，判断正确）
- 删掉 ReviewTimelineGrid / assignReviewColumns / reviewHeightEstimate / SVG route 全套（main.tsx 净减约 124 行）。
- Timeline 里删掉 group.records.length >= 4 判断，分列入口整个消失。
- 删掉 .review-timeline-grid .timeline-content 那套卡片样式（border + border-radius 14px + 白底 0.82 + box-shadow）—— 这就是「卡片化格式被改掉」。
- TimelineItem 把 .timeline-content 拆开：.timeline-meta 挪成 grid 第 3 列第 1 行，新增 .timeline-footer 包裹层，.timeline-content 只剩正文。
- **CDP 实测（1680 视口）**：09-14（14 条）distinctX=[352]、columnsDetected=1、reviewGrid=0、reviewRoute=0、dataReview=0；卡片宽 780（占满，不再半宽）。**双列 100% 没了。**
- 孤儿：is-reviewing-past 在 TSX 还在、CSS 已无对应规则（无害但脏）。

### 3. 未提交改动 B：编辑/删除 图标被推远（主人说「难评」的那条）
- 按钮文字标签被删：从 record-action 带「编辑」「删除」文字 → record-action-icon 纯图标 32×32（靠 aria-label / title 兜底）。
- 新增 .timeline-footer .timeline-actions { margin: 0 0 0 auto } 把按钮顶到卡片最右；footer 是 display:flex、.relation-row 吃 flex:1 → 内容在左、图标在最右，中间撑开大段空白。
- CDP 实测（09-14）：关联行右缘 1055，编辑图标 x=1063（横向差 8px）；但 vGap=37（图标比内容低 37px）。视觉上「放得很远」来自**整行 flex + margin-left:auto 顶到最右**，不是像素间距大。
- 判定：**这是设计选择不是 bug**（确实要过「右下角两个图标」），但代价是：文字标签丢失、纯图标可发现性下降、桌面点面积只有 32px（pointer:coarse 才 44px）。

### 4. 未提交改动 C：一批合规/健壮性修复（与 UI 无关，质量其实不错）
- server.ts 新增 assertImportReferences()：导入 bundle 是全量对象图，校验 ref 不悬空（unknown_entity / entity_type_mismatch / duplicate_reference / self_reference）；软删记录与回收站资产算「已知」。
- /api/weather 的 date 从裸正则改 parseDateQuery（正则放得过 2026-02-30，且该值当缓存 key）。
- export.ts 新增 jsonScalar() 处理 U+2028/U+2029（JSON 合法但 YAML 1.2 当换行，会撕裂 frontmatter）；escapeMarkdownPath 同源处理。
- AIAssistant.tsx panel 加 inert={!open}。
- backup-retention.ts 给 RetentionRunInput / TrashedBackupEntry 加 id，React key 从 fileName+prunedAt（双备份会重复）换成 id。

### 5. 没有编译层面的「改坏」
- npm run typecheck 三 workspace **全绿**。坏的是设计/形态，不是类型。

### 6. 未完成 / 遗留
- **核心矛盾**：changelog 里 opencode 自己写的是「方案 A：改成中央直线轴、分列不动」，但**它交付的是删除整个双列**，与自述不符 —— 这就是「改到一半改不下去」。
- 孤儿 class is-reviewing-past（TSX 有、CSS 无）。
- verify-review-mode.mjs 断言「3/2 分列」—— 分列已不存在，该脚本现在应该是红的（本轮未跑到，属未验证）。
- .timeline-content 的 --record-line 还在，但正文网格语义变了，照片网格高度需复核。
- **禁止 git checkout . / git reset --hard 回退**：A/B/C 三批改动混在一起，硬回退会把 C 那批好的修复一起抹掉。要回退只能按文件/按 hunk 挑。

### 7. 环境提示
- 3011（API）/ 5199（Web）预览**当时在线**。
- 我的临时探针写在 .review/tmp/，勘察完已删除。


## 2026-09-17 16:20 · 回看双列拆除后的收尾（第二轮）

**做了什么**：把上一位 AI（opencode）改了一半的时间轴卡片形态收尾。主人已定：双列不恢复、白底圆角卡片铺到全部记录、编辑/删除纯图标收进卡片右下角、卡片占满正文列。

**关键实现事实**：
- 卡片皮肤从 `.review-timeline-grid .timeline-content` 移到**无前缀**的 `.timeline-content` —— 这是「卡片化消失」的根因（皮肤原来只挂在双列容器上）。
- 图标「离得很远」是**归属问题**不是像素问题：`margin-left: auto` 原本参照 669px 正文列，卡片一存在就参照 14px 内边距。
- 宽度用「占满正文列」：照片网格 `width: fit-content` + 固定格子宽，给满宽度**不会**把 1 张图拉成整行；`.timeline-text` 保留 610px 上限。
- 删除双列残留后 `main.tsx` 3867 → 3789 行。

**顺手修的真实缺陷**：设置页「回收站备份」列表 `key={entry.fileName + entry.prunedAt}` → `entry.id ?? ...`。一次双写备份（local+s3）两行同名同 prunedAt，React 报重复 key（2 条 console error）。上一位 AI 已加 API 字段与 TS 类型，**只差渲染这一处**。

**两个「从没存在过」的 DOM 钩子**（重要教训）：`is-full-time` / `.timeline-time-toggle` 与时间轴项上的 `data-record-id` —— 逐 commit 扫过全仓 **34 个提交，在 `main.tsx`/`styles.css` 的任何历史版本里都不存在**，只存在于上一位 AI 的**未提交工作区** + 它写的验收脚本里。验收脚本引用的 DOM 钩子**必须先在 git 历史确认存在**。已把断言改成测真实渲染。

**验收数字**：typecheck 三 workspace 绿；npm test core 23/23 + API 47/47；verify-timeline-card 38/0 PASS；verify-review-mode 68/0 PASS；verify-ui-copy PASS（console errors 0）；verify-timeline-chip 25/25；verify-calendar PASS。

**其它坑**：
- `node -e ...`...`...` 内联反引号会被 bash 当命令替换 → 长文本先写文件再 append。跨 CDP 边界的正则用 `[0-9]` 显式区间，别用 `\d`（会被二次转义成字面量 d）。
- 验收脚本 `waitFor` 超时的失败形态是 **CRASH 而不是 FAIL**，且结果文件里会留着上一次的 PASS。


## 2026-09-17 18:45 · 卡片宽度改成「六图基准」428px

主人反馈「白色卡片太宽」，给出基准：**以六张照片的左右留白为准固定卡片宽度**，并要求靠左与时间轴对齐。

**改动**：
- `.timeline-content`：`max-width: calc(3 * 5 * 1.617rem + 2 * 6px + 28px)`（= 428.08px）+ `justify-self: start`。
- `.timeline-text`：**删掉 `max-width: 610px`**（卡片即行宽）。
- `.timeline-footer`：加 `flex-wrap`；新增 `.timeline-footer .relation-row { flex: 1 1 auto; min-width: 0; flex-wrap: wrap; margin-top: 0 }`。
- `main.tsx`：`{relationLine}` 从卡片直属子元素**移进 footer**。

**428px 的算法**：一行 3 格，格子 = 五行正文 = `5 × 1.617rem = 129.36px`，格间距 6px → 网格 400.08px；+ 两侧 14px 内边距 = 428.08px。六图恰好填满。旧值 669px 时六图右侧空 227px。

**收窄暴露的真问题**：胶囊原来独占一行，卡片变窄后把操作按钮挤出右缘（`editX=490 < chipsRight=559`）。移进 footer + `min-width: 0` 后修复（`editX=567`）。**给 flex 子项设 `min-width: 0` 才能让它真的收缩**，只写 `flex-shrink: 1` 不够。

**验收**：typecheck 三绿；npm test 23/23 + 47/47；verify-timeline-card **42/0**（新增 4 条宽度契约）；verify-review-mode 68/0；ui-copy / timeline-chip / calendar 全 PASS。
两个新探针跨 11 视口实测：`.review/probe-card-widths.mjs` —— 桌面段卡宽恒 428、左缘同一 x，560→410 / 520→403 / 430→313 / 390→273 平滑收缩，**全视口 overflows=0**；`.review/probe-six-fit.mjs` —— 6 格网格注入真实卡片，11 视口 **badCells=0**（leftover 恒为 1px，是 398.08 的亚像素取整）。

**教训**：`node -e ...`...`...` 里放 Markdown 反引号会被 bash 吃成命令替换（这次连 `\```` 代码围栏都炸了）。**长文本一律先 Write 到文件，再 `appendFileSync`**。

## 23:30 卡片宽度统一为固定值 + 图标钉右下角

- 主人裁定：**宽度统一 428（六图基准），高度随内容**；图标选**方案 B**（固定贴卡片右下角）。
- `.timeline-content`：`max-width` → `width` + `max-width:100%`。**只给 max-width 是错的**，没图的卡片会缩成包住文字（实测 188~282px 六种宽度）。
- `.timeline-footer`：flex → grid（`minmax(0,1fr) max-content`），actions 显式 `grid-column: 2`。
  自动排布会导致**没胶囊的记录**把按钮放进第 1 轨、差 8px。
- 验收：probe-corner **PASS(9)**（gapRight/gapBottom 全 1px）；probe-card-widths 11 视口 428 恒定、0 溢出；probe-six-fit badCells=0；verify-timeline-card 29/0；verify-review-mode 68/0；typecheck 绿；core 23/23、api 47/47。
- 提交：`54676d5`。

## 23:00 事故：C 盘被验收脚本泄漏的临时 Chrome profile 写满

- 症状：API 测试报 `database or disk is full`、node 报 `ENOSPC` —— **伪装成产品回归**。
- 实测 C 盘 100%（剩 1.7MB）；Temp 里 **178 个陈旧 profile、6.75GB**，全是 `.review/*.mjs` 里 `mkdtempSync` 没删。
- 已给 7 个泄漏脚本补 `rmSync(profile)`；全库扫描 **82 个脚本 0 泄漏**。
- **未执行删除**：按安全规矩，个人目录清理只报告不代删 —— 已把清单交给主人。
